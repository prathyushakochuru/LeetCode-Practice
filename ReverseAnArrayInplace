public class Solution {
    public void ReverseString(char[] s) {
       int i = 0;
       int len = s.Length-1;
        while(i<len)
        {
            char temp = s[i];
            s[i] = s[len];
            s[len] = temp;
            i++;
            len--;
        }
        
    }
}
